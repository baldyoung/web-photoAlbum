<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baldyoung.photoalbum.common.dao.RecordEmailDao">

    <insert id="insert" parameterType="map">
        INSERT INTO x_record_email(receiverEmail, senderEmail, emailContent, emailStatus)
        VALUES(#{param.receiverEmail}, #{param.senderEmail}, #{param.emailContent}, #{param.emailStatus})
    </insert>

</mapper>